from sqlmodel import Session, select & echo from backend.models.conversation_model import Conversation & echo. & echo def create_conversation(session: Session, conversation: Conversation): & echo     session.add(conversation) & echo     session.commit() & echo     session.refresh(conversation) & echo     return conversation & echo. & echo def get_conversation(session: Session, thread_id: str): & echo     return session.exec(select(Conversation).where(Conversation.thread_id == thread_id)).first()
